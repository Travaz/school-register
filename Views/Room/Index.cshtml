@model IEnumerable<school_register.Model.Entities.Room>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Floor)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Lim)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Floor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Lim)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NumeroAula">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NumeroAula">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NumeroAula">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
